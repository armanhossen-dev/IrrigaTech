I'll build the complete Flutter foundation scaffold. Let me set up the project structure and write each file.

File tree (27 files):

IrrigaTech/
├── PART1_NOTES.md
├── pubspec.yaml
└── lib/
    ├── main.dart
    ├── firebase_options.dart          (placeholder — needs flutterfire configure)
    ├── core/
    │   ├── constants.dart
    │   ├── theme.dart
    │   ├── router.dart
    │   └── utils/
    │       ├── validators.dart
    │       └── formatters.dart
    ├── data/
    │   ├── models/
    │   │   ├── sensor_data.dart
    │   │   ├── motor_state.dart
    │   │   ├── alert_log.dart
    │   │   └── weather_forecast.dart
    │   └── repositories/
    │       ├── irrigation_repository.dart   (abstract interface)
    │       ├── demo_data_repository.dart
    │       ├── blynk_repository.dart
    │       └── auth_repository.dart
    ├── domain/
    │   └── providers/
    │       └── repository_providers.dart
    └── presentation/
        └── screens/
            ├── splash_screen.dart          (full)
            ├── onboarding_screen.dart      (full)
            ├── login_screen.dart           (full)
            ├── device_setup_screen.dart    (full)
            ├── dashboard_screen.dart       (placeholder)
            ├── motor_detail_screen.dart    (placeholder)
            ├── analytics_screen.dart       (placeholder)
            ├── alerts_screen.dart          (placeholder)
            ├── security_log_screen.dart    (placeholder)
            └── settings_screen.dart        (placeholder)

Key design decisions worth knowing about: IrrigationRepository is the single interface both DemoDataRepository and BlynkRepository implement, and repositoryModeProvider (defaults to demo) is the one switch that flips the whole app between them — the Device Setup screen uses this to flip to live and call testConnection() for real. All route paths live in AppRoutes and pin numbers in AppConstants, so future parts shouldn't need to hardcode either.

PART1_NOTES.md has the full breakdown of what's built vs. still a placeholder, plus a table of exact class/file names for the next session to extend — worth skimming before starting Part 2.

-- 1